[build]
builder = "RAILPACK"

[deploy]
startCommand = "php -S 0.0.0.0:8080 -t ."
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[phases]
[phases.build]
commands = ["echo 'Building PayNex..."]

[phases.deploy]
commands = ["echo 'Deploying PayNex..."]
